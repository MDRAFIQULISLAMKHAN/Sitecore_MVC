@using System.Collections
@model Practice.Models.HeroBannerModel

<div class="welcome-area" id="welcome" style="background-image: @Model.BackgroundImage ">

    <!-- ***** Header Text Start ***** -->
    <div class="header-text">
        <div class="container">
            <div class="row">
                <div class="offset-xl-3 col-xl-6 offset-lg-2 col-lg-8 col-md-12 col-sm-12">
                    <h1>@Model.Title</h1>
                    <p>
                        @Model.Description
                    </p>
                    <a href="@Model.ButtonLink" class="main-button-slider">@Model.ButtonText</a>
                </div>
            </div>
        </div>
    </div>
    <!-- ***** Header Text End ***** -->
</div>